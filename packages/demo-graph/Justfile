default:
    @just --list

cli +ARGS:
    uv run --package demo-graph cli {{ARGS}}

demo:
    uv run --package demo-graph cli ingest Things

query +ARGS:
    uv run --package demo-graph cli query "{{ARGS}}"
